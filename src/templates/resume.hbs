<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Резюме - {{name}}</title>
  <style>
    :root {
      --primary-color: #4a6da7;
      --secondary-color: #304878;
      --text-color: #333;
      --light-color: #f8f9fa;
      --border-color: #ddd;
    }
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: var(--text-color);
      line-height: 1.6;
      background-color: #f5f5f5;
      padding: 20px;
    }
    
    .container {
      max-width: 1000px;
      margin: 0 auto;
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    
    header {
      background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
      color: white;
      padding: 40px;
      text-align: center;
    }
    
    .profile {
      display: flex;
      align-items: center;
      justify-content: center;
      flex-wrap: wrap;
      gap: 30px;
      margin-bottom: 20px;
    }
    
    .profile-image {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      border: 5px solid rgba(255, 255, 255, 0.3);
      object-fit: cover;
    }
    
    .profile-info h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
    }
    
    .profile-info p {
      font-size: 1.2rem;
      opacity: 0.9;
    }
    
    .contact-info {
      display: flex;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
    }
    
    .contact-item {
      display: flex;
      align-items: center;
      gap: 5px;
    }
    
    main {
      padding: 40px;
    }
    
    section {
      margin-bottom: 40px;
    }
    
    h2 {
      color: var(--primary-color);
      margin-bottom: 20px;
      border-bottom: 2px solid var(--border-color);
      padding-bottom: 10px;
    }
    
    .about-me {
      font-size: 1.1rem;
      line-height: 1.7;
    }
    
    .repositories {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
    }
    
    .repo-card {
      background-color: var(--light-color);
      border-radius: 8px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }
    
    .repo-card:hover {
      transform: translateY(-5px);
    }
    
    .repo-card h3 {
      margin-bottom: 10px;
      color: var(--secondary-color);
    }
    
    .repo-card p {
      color: #666;
      margin-bottom: 15px;
      min-height: 50px;
    }
    
    .repo-meta {
      display: flex;
      gap: 15px;
      font-size: 0.9rem;
      color: #777;
    }
    
    .repo-language {
      font-weight: bold;
      color: var(--primary-color);
    }
    
    footer {
      background-color: var(--light-color);
      text-align: center;
      padding: 20px;
      border-top: 1px solid var(--border-color);
      font-size: 0.9rem;
      color: #777;
    }
    
    .btn {
      display: inline-block;
      background-color: var(--primary-color);
      color: white;
      text-decoration: none;
      padding: 8px 20px;
      border-radius: 5px;
      transition: background-color 0.3s;
      font-weight: 500;
      margin-top: 10px;
    }
    
    .btn:hover {
      background-color: var(--secondary-color);
    }
    
    .github-stats {
      display: flex;
      justify-content: center;
      gap: 30px;
      flex-wrap: wrap;
      margin: 30px 0;
    }
    
    .stat-item {
      text-align: center;
      background-color: var(--light-color);
      padding: 15px 25px;
      border-radius: 8px;
      min-width: 120px;
    }
    
    .stat-value {
      font-size: 1.8rem;
      font-weight: bold;
      color: var(--primary-color);
    }
    
    .stat-label {
      font-size: 0.9rem;
      color: #777;
    }
    
    .social-links {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 20px;
      flex-wrap: wrap;
    }
    
    .social-link {
      padding: 8px 15px;
      background-color: rgba(255, 255, 255, 0.2);
      border-radius: 5px;
      text-decoration: none;
      color: white;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: background-color 0.3s;
    }
    
    .social-link:hover {
      background-color: rgba(255, 255, 255, 0.3);
    }
    
    @media (max-width: 768px) {
      .profile {
        flex-direction: column;
      }
      
      .repositories {
        grid-template-columns: 1fr;
      }
      
      header, main {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="profile">
        {{#if githubData.avatarUrl}}
          <img src="{{githubData.avatarUrl}}" alt="{{name}}" class="profile-image">
        {{else if photoUrl}}
          <img src="{{photoUrl}}" alt="{{name}}" class="profile-image">
        {{/if}}
        
        <div class="profile-info">
          <h1>{{name}}</h1>
          {{#if githubData.bio}}
            <p>{{githubData.bio}}</p>
          {{/if}}
          <p>{{location}}</p>
          {{#if githubData.company}}
            <p>{{githubData.company}}</p>
          {{/if}}
        </div>
      </div>
      
      <div class="contact-info">
        <div class="contact-item">
          <span>📧</span>
          <a href="mailto:{{email}}" style="color: white; text-decoration: none;">{{email}}</a>
        </div>
        
        {{#if github}}
        <div class="contact-item">
          <span>🌐</span>
          <a href="{{github}}" style="color: white; text-decoration: none;" target="_blank">GitHub</a>
        </div>
        {{/if}}
      </div>
      
      <div class="social-links">
        {{#if githubData.blog}}
          <a href="{{githubData.blog}}" class="social-link" target="_blank">🔗 Веб-сайт</a>
        {{/if}}
        
        {{#if githubData.twitter}}
          <a href="https://twitter.com/{{githubData.twitter}}" class="social-link" target="_blank">🐦 Twitter</a>
        {{/if}}
      </div>
    </header>
    
    <main>
      <section class="about-section">
        <h2>Обо мне</h2>
        <div class="about-me">
          {{#if githubData.bio}}
            <p>{{githubData.bio}}</p>
          {{else}}
            <p>Я разработчик, стремящийся к постоянному развитию и изучению новых технологий. 
            Моя цель — создавать качественные, эффективные и удобные для пользователей решения.</p>
          {{/if}}
        </div>
      </section>
      
      {{#if githubData}}
      <section class="github-section">
        <h2>Статистика GitHub</h2>
        <div class="github-stats">
          <div class="stat-item">
            <div class="stat-value">{{githubData.publicRepos}}</div>
            <div class="stat-label">Репозиториев</div>
          </div>
          <div class="stat-item">
            <div class="stat-value">{{githubData.followers}}</div>
            <div class="stat-label">Подписчиков</div>
          </div>
        </div>
      </section>
      {{/if}}
      
      {{#if repositories}}
      <section class="projects-section">
        <h2>Проекты</h2>
        <div class="repositories">
          {{#each repositories}}
          <div class="repo-card">
            <h3>{{name}}</h3>
            <p>{{description}}</p>
            <div class="repo-meta">
              {{#if language}}
              <span class="repo-language">{{language}}</span>
              {{/if}}
              <span>⭐ {{stars}}</span>
              <span>🍴 {{forks}}</span>
            </div>
            <a href="{{url}}" class="btn" target="_blank">Посмотреть</a>
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}
    </main>
    
    <footer>
      <p>Страница сгенерирована автоматически на основе данных GitHub API</p>
      <p>Последнее обновление: {{renderDate}}</p>
    </footer>
  </div>
</body>
</html> 